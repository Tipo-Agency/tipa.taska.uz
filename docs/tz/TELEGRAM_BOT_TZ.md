# Техническое задание: Telegram бот для системы управления задачами

## 1. Общее описание

Telegram бот для интеграции с веб-приложением системы управления задачами. Бот предоставляет доступ к основным функциям системы через Telegram интерфейс.

**Версия:** 1.0  
**Дата:** 2026-01-24  
**Статус:** Активная разработка

---

## 2. Функциональные требования

### 2.1. Аутентификация и авторизация

#### 2.1.1. Команда `/start`
- **Описание:** Начало работы с ботом
- **Сценарий:**
  1. Пользователь отправляет `/start`
  2. Если пользователь уже авторизован → показывается главное меню
  3. Если не авторизован → запрашивается логин
  4. После ввода логина → запрашивается пароль
  5. После успешной авторизации → показывается главное меню
- **Обработка ошибок:**
  - Неверный логин → сообщение об ошибке, повторный запрос логина
  - Неверный пароль → сообщение об ошибке, повторный запрос пароля
  - Пользователь архивирован → сообщение об ошибке, отказ в доступе

#### 2.1.2. Команда `/logout`
- **Описание:** Выход из системы
- **Сценарий:**
  1. Пользователь отправляет `/logout`
  2. Сессия удаляется из `user_sessions`
  3. Показывается сообщение об успешном выходе
  4. При следующем `/start` требуется повторная авторизация

#### 2.1.3. Декоратор `@require_auth`
- **Описание:** Проверка авторизации перед выполнением функции
- **Логика:**
  - Проверяет наличие сессии в `user_sessions`
  - Проверяет активность пользователя через `check_user_active`
  - Если не авторизован → отправляет сообщение с предложением авторизоваться

---

### 2.2. Главное меню

#### 2.2.1. Структура меню
- 📋 Мои задачи
- 🎯 Все сделки
- ⚙️ Настройки
- 👤 Профиль
- 🌐 Открыть веб-приложение (Web App)
- ❓ Помощь

#### 2.2.2. Команда `/help`
- **Описание:** Показывает справку по использованию бота
- **Содержание:**
  - Основные команды (`/start`, `/logout`, `/help`)
  - Описание функций меню
  - Инструкции по использованию

---

### 2.3. Работа с задачами

#### 2.3.1. Меню задач
**Кнопки:**
- 📋 Задачи на сегодня
- ⚠️ Просроченные
- 📊 Все задачи
- ➕ Создать задачу
- 🔙 Назад

#### 2.3.2. Задачи на сегодня (`tasks_today`)
- **Описание:** Показывает задачи пользователя с дедлайном на сегодня
- **Фильтрация:**
  - Только задачи текущего пользователя (`assigneeId` или `assigneeIds`)
  - Только с `endDate` = сегодня
  - **Исключаются:** задачи со статусом "Выполнено", "Done", "Завершено", "Completed" (case-insensitive)
  - **Исключаются:** архивные задачи (`isArchived = true`)
- **Формат вывода:**
  - Если задач нет → "✅ Задач на сегодня нет"
  - Если есть → список задач с форматированием через `format_task_message`

#### 2.3.3. Просроченные задачи (`tasks_overdue`)
- **Описание:** Показывает просроченные задачи пользователя
- **Фильтрация:**
  - Только задачи текущего пользователя
  - Только с `endDate` < сегодня (через `is_overdue`)
  - **Исключаются:** задачи со статусом "Выполнено", "Done", "Завершено", "Completed"
  - **Исключаются:** архивные задачи
- **Формат вывода:**
  - Если задач нет → "✅ Просроченных задач нет"
  - Если есть → список задач

#### 2.3.4. Все задачи (`tasks_all`)
- **Описание:** Показывает все задачи пользователя
- **Фильтрация:**
  - Только задачи текущего пользователя
  - **Исключаются:** задачи со статусом "Выполнено", "Done", "Завершено", "Completed"
  - **Исключаются:** архивные задачи
- **Формат вывода:**
  - Если задач нет → "✅ Задач нет"
  - Если есть → список задач (ограничение: первые 20)

#### 2.3.5. Создание задачи (`task_create`)
- **Описание:** Создание новой задачи через ConversationHandler
- **Сценарий:**
  1. Пользователь нажимает "➕ Создать задачу"
  2. Запрашивается название задачи
  3. Запрашивается срок выполнения (дата в формате ДД.ММ.ГГГГ или ДД.ММ)
  4. Запрашивается исполнитель (выбор из списка пользователей)
  5. Задача создается в Firebase
  6. Показывается подтверждение
- **Валидация:**
  - Название не может быть пустым
  - Дата должна быть валидной
  - Исполнитель должен быть выбран из списка активных пользователей
- **Обработка ошибок:**
  - Ошибка создания → сообщение об ошибке
  - Отмена → возврат в меню задач

#### 2.3.6. Детали задачи (`task_detail`)
- **Описание:** Показывает детальную информацию о задаче
- **Кнопки:**
  - 📊 Изменить статус
  - 💬 Добавить комментарий
  - 🔙 Назад

#### 2.3.7. Изменение статуса задачи (`task_set_status`)
- **Описание:** Изменение статуса задачи
- **Сценарий:**
  1. Пользователь нажимает "📊 Изменить статус"
  2. Показывается список доступных статусов
  3. Пользователь выбирает статус
  4. Статус обновляется в Firebase
  5. Показывается подтверждение

---

### 2.4. Работа со сделками

#### 2.4.1. Меню сделок
**Кнопки:**
- 🎯 Все сделки
- 👤 Мои заявки
- ➕ Создать заявку
- 🔍 Поиск
- 🔙 Назад

#### 2.4.2. Все сделки (`deals_all`)
- **Описание:** Показывает список всех сделок
- **Фильтрация:**
  - **Исключаются:** архивные сделки (`isArchived = true`) по умолчанию
- **Сценарий:**
  1. Показывается список воронок продаж
  2. Пользователь выбирает воронку (или "Все воронки")
  3. **Показывается выбор этапа воронки** (новое требование)
  4. Пользователь выбирает этап (или "Все этапы")
  5. Показываются сделки выбранной воронки и этапа
- **Кнопки для каждой сделки:**
  - ✏️ Редактировать
  - 📊 Изменить стадию
  - 📋 Создать задачу
  - 🗑️ В архив
  - 🔙 Назад

#### 2.4.2.1. Выбор этапа воронки (`deals_funnel_stage`)
- **Описание:** Показывает сделки по выбранной воронке и этапу
- **Сценарий:**
  1. После выбора воронки показывается список этапов
  2. Пользователь выбирает этап (или "Все этапы")
  3. Показываются сделки выбранной воронки и этапа
- **Обработчик:** `deals_funnel_stage` с паттерном `^deals_funnel_stage_`

#### 2.4.3. Новые заявки (`deals_new`)
- **Описание:** Показывает все заявки на этапе "Новая заявка"
- **Фильтрация:**
  - Только сделки с `stage` содержащим "новая" или "new" (case-insensitive)
  - **Исключаются:** архивные сделки
- **Использование:** Кнопка "🆕 Новые заявки" в меню сделок

#### 2.4.4. Мои заявки (`deals_mine`)
- **Описание:** Показывает сделки, назначенные на текущего пользователя
- **Фильтрация:**
  - Только сделки с `assigneeId` = текущий пользователь
  - **Исключаются:** архивные сделки

#### 2.4.5. Создание сделки (`deal_create`)
- **Описание:** Создание новой сделки через ConversationHandler
- **Сценарий:**
  1. Пользователь нажимает "➕ Создать заявку"
  2. Показывается список воронок продаж
  3. Пользователь выбирает воронку
  4. Показывается список стадий выбранной воронки
  5. Пользователь выбирает стадию
  6. Запрашивается название сделки
  7. Запрашивается описание (опционально)
  8. Сделка создается в Firebase
  9. Показывается подтверждение
- **Валидация:**
  - Воронка должна быть выбрана
  - Стадия должна быть выбрана
  - Название не может быть пустым

#### 2.4.6. Удаление сделки в архив (`deal_delete`)
- **Описание:** Мягкое удаление сделки (архивирование)
- **Сценарий:**
  1. Пользователь нажимает "🗑️ В архив"
  2. Показывается подтверждение
  3. Если подтверждено → `isArchived = true` в Firebase
  4. Показывается подтверждение

#### 2.4.7. Изменение стадии сделки (`deal_set_stage`)
- **Описание:** Изменение стадии сделки
- **Сценарий:**
  1. Пользователь нажимает "📊 Изменить стадию"
  2. Показывается список стадий текущей воронки
  3. Пользователь выбирает стадию
  4. Стадия обновляется в Firebase
  5. Показывается подтверждение

---

### 2.5. Настройки

#### 2.5.1. Меню настроек
**Кнопки:**
- 🔔 Уведомления
- 🔙 Назад

#### 2.5.2. Настройки уведомлений (`settings_notifications`)
- **Описание:** Главное меню настроек уведомлений
- **Категории:**
  - 📋 Задачи
  - 📄 Документы
  - 📅 Встречи
  - 🎯 CRM (Сделки, Клиенты)
  - 💰 Финансы
  - 👥 Групповой чат
  - 🔙 Назад

#### 2.5.3. Настройки уведомлений по категориям
- **Задачи (`settings_notif_tasks`):**
  - Новая задача (`newTask`)
  - Изменение статуса (`statusChange`)
  - Назначение задачи (`taskAssigned`)
  - Комментарий к задаче (`taskComment`)
  - Дедлайн задачи (`taskDeadline`)
- **Документы (`settings_notif_docs`):**
  - Создан документ (`docCreated`)
  - Обновлен документ (`docUpdated`)
  - Документ расшарен (`docShared`)
- **Встречи (`settings_notif_meetings`):**
  - Создана встреча (`meetingCreated`)
  - Напоминание о встрече (`meetingReminder`)
  - Обновлена встреча (`meetingUpdated`)
- **CRM (`settings_notif_crm`):**
  - Новая сделка (`dealCreated`)
  - Изменение статуса сделки (`dealStatusChanged`)
  - Новый клиент (`clientCreated`)
  - Новый договор (`contractCreated`)
- **Финансы (`settings_notif_finance`):**
  - Новая заявка на покупку (`purchaseRequestCreated`)
  - Изменение статуса заявки (`purchaseRequestStatusChanged`)
  - Обновлен финансовый план (`financePlanUpdated`)

#### 2.5.4. Переключение уведомлений (`settings_toggle_notification`)
- **Описание:** Переключение конкретной настройки уведомления
- **Логика:**
  - Переключает `telegramPersonal` для выбранной настройки
  - Сохраняет изменения в Firebase (`notificationPrefs` с `id: 'default'`)
  - Возвращает в соответствующую категорию настроек

#### 2.5.5. Настройки группового чата (`settings_notif_group`)
- **Описание:** Настройка ID группового чата для уведомлений
- **Функции:**
  - Показ текущего `telegramGroupChatId`
  - Кнопка "📝 Ввести ID группового чата"
  - Инструкция по получению ID через `/group_id`

#### 2.5.6. Установка ID группового чата (`settings_group_set_chat_id`)
- **Описание:** ConversationHandler для ввода ID группового чата
- **Сценарий:**
  1. Пользователь нажимает "📝 Ввести ID группового чата"
  2. Запрашивается ID
  3. ID сохраняется в `notificationPrefs.telegramGroupChatId`
  4. Показывается подтверждение

#### 2.5.7. Команда `/group_id`
- **Описание:** Получение ID текущего чата (для групп)
- **Использование:** Отправляется в группе, где находится бот
- **Вывод:** ID чата в формате: "ID этого чата: -123456789"

---

### 2.6. Профиль

#### 2.6.1. Меню профиля
**Кнопки:**
- 🔑 Изменить пароль
- 🖼️ Изменить аватарку
- 📞 Изменить контакты
- 🔙 Назад

#### 2.6.2. Просмотр профиля (`menu_profile`)
- **Описание:** Показывает информацию о текущем пользователе
- **Формат:** Использует `format_profile_message` из `profile.py`

---

### 2.7. Работа в группах

#### 2.7.1. Создание задачи из упоминания бота
- **Описание:** Создание задачи из сообщения в группе, где упомянут бот
- **Сценарий:**
  1. Пользователь упоминает бота в группе: "@bot_name Создать задачу: Название"
  2. Бот инициирует ConversationHandler
  3. Запрашивается срок выполнения
  4. Запрашивается исполнитель
  5. Задача создается
  6. Показывается подтверждение в группе

#### 2.7.2. Команды для показа сущностей в группе

##### `/task <id или название>`
- **Описание:** Показывает задачу в группе
- **Логика поиска:**
  1. Сначала поиск по точному ID
  2. Если не найдено → поиск по частичному совпадению названия (case-insensitive)
  3. Если найдено несколько → показывается список с ID для выбора
  4. Если найдено одно → показывается детальная информация
- **Ограничения:**
  - Работает только в группах (`chat.type in ['group', 'supergroup']`)
  - Исключаются архивные задачи

##### `/deal <id или название>`
- **Описание:** Показывает сделку в группе
- **Логика:** Аналогична `/task`
- **Ограничения:** Работает только в группах, исключаются архивные сделки

##### `/meeting <id или название>`
- **Описание:** Показывает встречу в группе
- **Логика:** Аналогична `/task`
- **Ограничения:** Работает только в группах, исключаются архивные встречи

##### `/document <id или название>`
- **Описание:** Показывает документ в группе
- **Логика:** Аналогична `/task`
- **Ограничения:** Работает только в группах, исключаются архивные документы

---

### 2.8. Уведомления и планировщик

#### 2.8.1. Периодическая проверка (`periodic_check`)
- **Описание:** Проверка новых задач, сделок и встреч каждые 30 секунд
- **Логика:**
  - Проверяет новые задачи для каждого авторизованного пользователя
  - Проверяет новые сделки
  - Проверяет предстоящие встречи (за 15 минут)
  - Отправляет уведомления согласно настройкам `notificationPrefs`

#### 2.8.2. Ежедневные напоминания
- **Время:** 9:00 (Asia/Tashkent)
- **Описание:** Отправка личных напоминаний о задачах на сегодня и просроченных
- **Получатели:** Все активные пользователи с настроенным `telegramUserId`

#### 2.8.3. Ежедневная сводка в группу
- **Время:** 9:05 (Asia/Tashkent)
- **Описание:** Отправка сводки по задачам в групповой чат
- **Содержание:**
  - Задачи на вчера (не выполненные)
  - Ранее просроченные задачи
  - Задачи на сегодня с ответственными
- **Получатель:** Групповой чат из `notificationPrefs.telegramGroupChatId`

#### 2.8.4. Уведомления об успешных сделках
- **Описание:** Отправка уведомления в группу при переходе сделки в стадию "won"
- **Триггер:** Проверка в `periodic_check` или при изменении стадии сделки
- **Формат:** "🎉 Всем привет, поздравляю! У нас новый клиент!" + детали сделки

#### 2.8.5. Уведомления администраторам при создании заявки
- **Описание:** Отправка уведомления всем администраторам при создании новой заявки
- **Триггер:** Создание сделки через веб-приложение или бота
- **Получатели:** Все пользователи с `role === 'ADMIN'` и `telegramUserId` настроен
- **Формат:** "🆕 Новая заявка" + название, стадия, сумма, создатель, ответственный
- **Реализация:** В `notifyDealCreated` в `services/notificationService.ts`

#### 2.8.6. Еженедельный отчет
- **Время:** Понедельник, 9:00 (Asia/Tashkent)
- **Описание:** Отправка статистики за неделю в групповой чат
- **Содержание:**
  - Количество выполненных задач
  - Количество просроченных задач
  - Топ-5 лучших сотрудников
  - Список сотрудников, которым нужно улучшить показатели

---

## 3. Технические требования

### 3.1. Структура проекта

```
telegram-bot/
├── bot.py              # Главный файл с обработчиками
├── auth.py             # Аутентификация
├── keyboards.py        # Клавиатуры
├── messages.py         # Форматирование сообщений
├── tasks.py            # Работа с задачами
├── deals.py            # Работа со сделками
├── clients.py          # Работа с клиентами
├── profile.py          # Профиль пользователя
├── notifications.py    # Уведомления
├── scheduler.py        # Планировщик задач
├── firebase_client.py  # Клиент Firebase
├── config.py           # Конфигурация
└── utils.py            # Утилиты
```

### 3.2. Зависимости

- `python-telegram-bot` (версия 20.x)
- `firebase-admin` или REST API клиент
- `apscheduler` для планировщика
- `bcrypt` для хеширования паролей
- `pytz` для работы с часовыми поясами

### 3.3. Состояния ConversationHandler

```python
LOGIN = 0
PASSWORD = 1
TASK_FROM_MESSAGE_TITLE = 2
TASK_FROM_MESSAGE_DATE = 3
TASK_FROM_MESSAGE_ASSIGNEE = 4
SETTING_GROUP_CHAT_ID = 5
```

### 3.4. Хранилище сессий

```python
user_sessions = {
    telegram_user_id: {
        'user_id': str,
        'last_check': datetime
    }
}
```

### 3.5. Порядок регистрации обработчиков

**КРИТИЧЕСКИ ВАЖНО:** Порядок регистрации определяет приоритет обработки.

1. **CommandHandler'ы** (команды `/start`, `/logout`, `/help`, `/group_id`, `/task`, `/deal`, `/meeting`, `/document`)
2. **ConversationHandler'ы** (авторизация, создание задач/сделок, настройки)
3. **CallbackQueryHandler'ы** (обработка кнопок)
4. **MessageHandler для текстовых сообщений** (последним, чтобы не перехватывать команды)

---

## 4. Обработка ошибок

### 4.1. Общие принципы

1. **Все функции должны быть обернуты в try-except**
2. **Логирование ошибок:** `logger.error(f"Error in {function_name}: {e}", exc_info=True)`
3. **Пользовательские сообщения:** При ошибке отправляется понятное сообщение пользователю
4. **Graceful degradation:** При ошибке бот не должен падать, а продолжать работать

### 4.2. Типичные ошибки

- **Ошибки Firebase:** Логирование + сообщение "Произошла ошибка при получении данных"
- **Ошибки валидации:** Сообщение с описанием проблемы + повторный запрос
- **Ошибки авторизации:** Сообщение "Неверный логин/пароль" + повторный запрос
- **Ошибки Telegram API:** Логирование + попытка отправить сообщение об ошибке (если возможно)

---

## 5. Тестирование

### 5.1. Критические сценарии

1. ✅ Авторизация (успешная и неуспешная)
2. ✅ Просмотр задач (сегодня, просроченные, все)
3. ✅ Создание задачи
4. ✅ Просмотр сделок
5. ✅ Создание сделки
6. ✅ Удаление сделки в архив
7. ✅ Настройки уведомлений
8. ✅ Работа в группах (команды `/task`, `/deal`, `/meeting`, `/document`)
9. ✅ Создание задачи из упоминания бота
10. ✅ Планировщик (ежедневные напоминания, сводки)

### 5.2. Граничные случаи

- Пустые списки (нет задач, сделок)
- Неверные ID при поиске
- Множественные результаты при поиске
- Архивные сущности (не должны показываться)
- Выполненные задачи (не должны показываться в фильтрах)

---

## 6. Логирование

### 6.1. Уровни логирования

- **INFO:** Основные события (старт бота, авторизация, создание задач)
- **WARNING:** Предупреждения (неверные данные, отсутствие настроек)
- **ERROR:** Ошибки (исключения, сбои)

### 6.2. Формат логов

```
%(asctime)s - %(name)s - %(levelname)s - %(message)s
```

### 6.3. Безопасность

- **НЕ логировать:** Токены, пароли, чувствительные данные
- **Логировать:** ID пользователей, ID задач/сделок, действия пользователей

---

## 7. Версионирование

### 7.1. Формат версии

```
YYYY-MM-DD-vN
```

Пример: `2026-01-21-v7`

### 7.2. Обновление версии

- Версия должна обновляться при каждом значительном изменении
- Версия логируется при старте бота
- Версия помогает отслеживать, что код обновился на сервере

---

## 8. Деплой и мониторинг

### 8.1. Деплой

- Бот запускается через systemd service
- При деплое проверяется версия кода в логах
- При ошибках бот автоматически перезапускается

### 8.2. Мониторинг

- Проверка логов на ошибки
- Проверка активности бота (ответ на `/start`)
- Проверка планировщика (ежедневные задачи выполняются)

---

## 9. Известные ограничения

1. **Хранилище сессий:** В памяти (в продакшене рекомендуется Redis)
2. **Ограничение списков:** Максимум 20 элементов в списках
3. **Поиск:** Только по ID и частичному совпадению названия
4. **Часовой пояс:** Фиксированный (Asia/Tashkent)

---

## 10. Планы на будущее

- [ ] Интеграция с Redis для сессий
- [ ] Расширенный поиск (по описанию, тегам)
- [ ] Поддержка нескольких часовых поясов
- [ ] Статистика использования бота
- [ ] Экспорт данных через бота
